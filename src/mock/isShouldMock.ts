const methods = [ 'get', 'post', 'put', 'delete' ]

const apis = [
  '/\\/list\\/table/'
]

const res= []
// apis.forEach((api) => {
//   res.push(...methods.map(method => `${api}/${method}`))
// })
// console.log(res)
export default res
