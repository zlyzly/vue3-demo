import request from '@/utils/request'

// test
export function getUserInfo(): void {
  // return new Promise((resolve, reject) => {
    //做一些异步操作
    setTimeout(function () {
      return '改变后的数据zly'
    }, 2000)
  // })
}
